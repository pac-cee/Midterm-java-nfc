package com.nfcpay.model.enums;

/**
 * Transaction Status Enumeration
 * Matches database status column values
 */
public enum TransactionStatus {
    PENDING,
    SUCCESS,
    FAILED,
    CANCELLED
}